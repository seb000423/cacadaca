# 문구·그림 전면 검수 — polytwin_UI

**검수일** 2026-08-31
**대상** `index.html` · `sub.html`(서브페이지 18) · `monitor.html` · `console-src/template.html` · `library-src/template.html` · `save-src/template.html`
**방법** 카피 전문 추출 후 통독 · 그림 24장 실물 열람 · 폰트 글리프 커버리지 실측 · 자동 점검(`scripts/review-check.py`)

> **편집 위치 주의.** `index` · `sub` · `monitor` 는 직접 고친다.
> `PolyTwin Console/Library/Save.html` 은 번들 결과물이라 **직접 고치면 안 된다** —
> `*-src/template.html` 을 고치고 `python *-src/repack.py` 로 되돌려 넣는다.

---

## 요약

| 등급 | 건수 | 성격 |
|---|---|---|
| **A. 치명** | 9 | 제품명 오타 · 학생 라이선스 노출 · 수치 정면 모순 |
| **B. 그림 속 오타·비문** | 12 | 이미지 안에 박혀 있어 재생성/재편집 필요 |
| **C. 그림 자체** | 9 | 톤 이탈 · 캡션 불일치 · 상표 노출 |
| **D. 카피 표기** | 9 | 같은 것을 두세 가지로 적는다 |
| **E. 기술·구조** | 5 | 폰트 폴백 · 고아 자산 |

**가장 먼저 고칠 3가지** — A-1(폴리트원) · A-2(STUDENT 워터마크) · A-3(접촉력 5가지).

---

## 처리 현황 — 2026-08-31 적용

**원칙** 접촉력 값(A-3·A-4·A-5)은 팀 확정 전까지 보류. 그 외는 전부 적용했다.
그림 속 글자는 픽셀을 배경색으로 덮고 Pretendard 로 다시 그렸다. 편집 전 원본은
`assets/img/_prepatch_2026-08-31/` 에 있고, 적용 스크립트는 멱등이라 원본에서 다시 돌릴 수 있다.

| 항목 | 상태 | 처리 |
|---|---|---|
| A-1 폴리트원 ×3 | ✅ | `wide_market`(헤드라인 + 본문 "폴리 트윈") · `wide_loop` · `crop_rl_policy` 픽셀 교체 |
| A-2 STUDENT 워터마크 | ✅ | `crop_ansys` — STUDENT · `2024-08-27 11:03` 영역을 배경색으로 덮음. 로고·`2024 R1`·해석 메타데이터는 유지 |
| A-3 접촉력 5종 | ⏸ 보류 | 팀 확정 후. `crop_force` · `crop_monitor` 는 데이터셋 trace 기반 네이티브 도해로 교체 권장 |
| A-4 목표 1.5 vs 유지 3–8 | ⏸ 보류 | 표기만 en dash 로 통일. `seg_best_kpi.json` 실측은 3.29–8.22 N · 중앙값 4.46 |
| A-5 48개 환경 변수 불일치 | ⏸ 보류 | `crop_rl_env` 교체 대기 — 본문(경로 패턴 × 이송 속도 % × 누름 강도 N)이 기준 |
| A-6 웨이포인트 813/984/6K | ✅ | 그림 축은 손대지 않고 캡션 4곳에 "x축: 초기 실행 984점" · "⑤ 축은 원본 실행 6,704점 기준" 명시 |
| A-7 media 자기모순 | ✅ | lede → "실행 화면 둘은 녹화 원본이고, 히어로 필름 하나만 연출 컷이다" |
| A-8 연 393만 | ✅ | 연 **396만 원** (2곳) |
| A-9 랜딩 사양 | ✅ | **1.41 MB / 3D 모델 4종 합계** · **93.5 % / 차체 모델 압축률**(4,097→264 KB 실측) · 폰트 **192 KB · 928자**(서브셋 재생성 반영) · 주석 갱신 |
| B-1 톨(N) ×5 | ✅ | `wide_loop` 4곳 → **힘(N)** (`wide_system` 이 "힘 (N)" 이라 근거 확보) · `crop_monitor` → **툴: POL-125**(공구 ID 이므로 툴) |
| B-2 폴리토일 | ✅ | `wide_rl_overview` → 폴리트윈 환경 |
| B-3 말풍선 비문 | ✅ | → "이 부위는 힘을 조금 더 주고 / 여러 번 지나가야 해요." |
| B-4 표면법·충동도·호차 | ✅ | 법선 편차 · 경로 충족도 · 오차 12.4% |
| B-5 레일 후에도 | ✅ | 레일 **이동** 후에도 |
| B-6 패드 직경 중복 | ✅ | 두 번째 → **작업 영역 (mm)** (`wide_loop` 의 같은 자리 라벨과 일치) |
| B-7 자동차단면 | ✅ | 자동차 단면 |
| B-8 협력,경쟁사 | ✅ | 협력사·경쟁사에게 |
| B-9 깨진 `—ㅓ` | ✅ | 제거 |
| B-10 제어/실행 | ✅ | 그림 설명문 + `?p=faq` → **제어** 로 통일 |
| B-11 (b) 배지 | ✅ | 제거, 제목을 왼쪽으로 당김 |
| B-12 kp 아래첨자 | ✅ | 그림은 p 글리프를 축소·하강(같은 폰트 유지), 카피는 `k<sub>p</sub>` (불릿은 HTML 허용 확인) |
| C-1 밝은 배경 2장 | 🔶 정정 | **`wide_docs` 는 오판** — 원본이 투명 배경(RGBA) webp 라 다크 UI 에서 원래 정상이었다. 검수 뷰어가 투명을 흰색으로 보여준 것. 다크 변환 시도가 오히려 가장자리를 깨뜨려 **원본 복원**. **`pdf_20` 팀 사진은 미처리** — 교체·삭제는 팀 판단 |
| C-2 blindspot 빈 공간 | ✅ | 1800×772 → **1800×560** 크롭, `DIM` 갱신 |
| C-3 AI 인물 | ✅ | `crop_rl_eval` 인물 영역 잘라냄 → **960×610**, `DIM` 갱신. "Expert Evaluation" → 전문가 평가 |
| C-4 pdf_20 톤 | ⏸ | 팀 판단 (실명·얼굴 동의 포함) |
| C-5 영문 라벨 | ✅ | 연마 패드 · 연마 대상 · 전문가 평가 · 충돌 위험 (Blind Spot 은 본문도 영문이라 유지) |
| C-6 캡션 불일치 | ✅ | sanding `crop_path` 캡션 → "국소 평면·법선 추정 — 영역별 경로의 기준면". deburring `crop_boundary` 는 경계 추종 경로로 읽혀 유지 |
| C-7 loop 캡션 중복 | ✅ | physical-ai → "Isaac Sim 위에서 도는 10단계". 추가로 sanding/deburring 스캔 캡션, pickplace/media 영상 캡션도 분리 |
| C-8 상표 | 🔶 부분 | `wide_system` "BMW Z4" → **세단 A**. 렌더 속 엠블럼·사진 속 Samsung/두산 로고는 픽셀 편집으로 불가 |
| C-9 PR 페이지 그림 0장 | ⏸ | 디자인 결정 |
| D-1 3~8/3–8/3.0–8.0 | ✅ | 카피의 숫자 범위 전부 **en dash** (sub 13 · index 2 · monitor 1 · library 1). 코드 주석은 그대로 |
| D-2 150mm | ✅ | 150 mm |
| D-3 ROS2 | ✅ | ROS 2 (3곳) |
| D-4 ANSYS | ✅ | 카피 5곳 → Ansys. 그림 `crop_ansys` 제목 "1. ANSYS 사전 검증" 은 유지 |
| D-5 5.6N/5.2N | ✅ | 5.6 N · 30 mm/s · 5.2 N |
| D-6 제어/실행 | ✅ | B-10 과 함께 |
| D-7 존댓말 | ✅ | ①사전설정 13문장 · ③저장 1문장 · ④라이브러리 3문장 → 평어체 (브라우저 검수에서 8건 추가 발견). 폼 검증·에러 메시지는 존댓말 유지 (규칙: **폼/에러만 존댓말**) |
| D-8 1.50 / 1.2 | 🔶 부분 | `crop_security` 1.2 → **1.20**(윗줄 '0' 글리프 복사). 카피 "1.50 N" 은 접촉력 보류에 묶임 |
| D-9 팀원 순서 | ✅ | news → values 순서(정용준·신현호·부승언·김두용) |
| E-1 폰트 103자 누락 | ✅ | 서브셋 **928자 · 192 KB** 재생성, 누락 **0**. `scripts/font-subset.py` 신설 — 카피 고칠 때마다 실행 |
| E-2 ASSETS.md 632/825 | ✅ | 928자·192 KB 로 갱신 + 재생성 절차·`full-ko` 기호 부재 주의 추가 |
| E-3 미참조 이미지 | ✅ | `assets/img` 23 + `icons` 4 → `assets/img/_unused/` 격리, `.vercelignore` 등록. `new_img/` 는 그대로(이미 배포 제외) |
| E-4 newsletter | ✅ | 데이터 삭제 |
| E-5 PH() 6건 | ⏸ | 결정 필요 |

**부수 조치**
- 브라우저 검수(Playwright, 22페이지 뷰포트 캡처)로 재확인 — `wide_market` 은 폰트가 달라 Pretendard 로 다시 그리면 튀어 **픽셀 수술**(원→윈은 ㅓ 가로획만 제거, "폴리 트윈"은 공백 칸만큼 밀기)로 교체. `wide_docs` 는 처리 자체가 오판이라 원본 복원
- 캡션 원문자 ①~⑤ 는 11px 에서 ⊙ 처럼 보여 사이트 관례인 `01`~`05` 표기로 통일
- 브라우저 검수 2차에서 잡은 것: `wide_loop` ② 제목이 배지 원과 붙음(글자 박스에 원이 섞임) · "로봇 배치" 행 윗부분 깎임 · `crop_rl_eval` 헤더 덮개 자국 → 덮개를 열 단위 상하 샘플링(그라데이션 보간)으로 바꿔 해결. ①사전설정 차체 리프트 안내 등 존댓말 8문장 추가 평어체
- 검수 방법 메모: 뷰포트 스크롤 캡처는 진입 애니메이션 중 저품질 래스터를 찍어 이미지 가장자리가 계단처럼 보일 수 있다 — 이미지 품질 판단은 **요소 단위 캡처**(`locator.screenshot`)로 할 것. `wide_docs` 계단은 그 아티팩트였고 실제 렌더는 정상
- `℃ ↔ −` 는 Pretendard 어느 원본에도 글리프가 없어 폴백을 유발한다 → 카피에서 `°C` · `/` · `–` 로 치환 (12곳)
- `ASSET_V` → `2026-08-31` (그림 캐시 무효화)
- 번들 3장은 `*-src/template.html` 수정 후 `repack.py` 로 재생성했다
- 옛 폰트 서브셋은 `assets/fonts/_prepatch_2026-08-31/` 에 보관 — **지우지 말 것.** `full-ko` 에 없는 기호 글리프의 유일한 공급원이다

**남은 결정 4개** — A-3/4/5(접촉력·48개 환경) · C-1/4(팀 사진) · C-9(PR 그림) · E-5(빈 자리)

---

# A. 치명 — 사실관계와 신뢰도

## A-1. 제품명이 그림 안에서 "폴리트**원**" 으로 잘못 적혀 있다 ★최우선

**3건. 전부 이미지 내부라 이미지를 다시 만들어야 한다.**

| 파일 | 위치 | 현재 | 맞는 표기 |
|---|---|---|---|
| `wide_market.webp` | 헤드라인 | 폴리트**원**은 3.2배 빠른 쪽에 올라탔다 | 폴리트**윈** |
| `wide_loop.webp` | ② 패널 제목 | 폴리트**원** 공정 실행 | 폴리트**윈** |
| `crop_rl_policy.webp` | 우측 박스 | 폴리트**원** 환경 48종 | 폴리트**윈** |

- 같은 `wide_market.webp` 본문에는 **"폴리 트윈은 그 시장에 올라탔다"** — 이번엔 띄어쓰기 오류다. 한 이미지 안에서 제품명을 두 번 다르게 틀렸다.
- `crop_ansys.webp` 에는 **"폴리트윈"** 이 올바르게 적혀 있어, 위 3건이 오타임이 확정된다.
- 텍스트 카피(`sub.html`)의 "폴리트윈" 은 전부 정상이다. **그림만 틀렸다.**

## A-2. Ansys **STUDENT 라이선스** 워터마크가 노출돼 있다 ★최우선

`crop_ansys.webp` 우측 상단에 `Ansys / 2024 R1 / **STUDENT**` 가 선명하다.
`?p=values` 와 `?p=physical-ai` 두 페이지에 실린다.

- 상업 제품 소개·투자 자료에 학생용 라이선스 표기가 그대로 나가면 **"교차 검증했다"는 주장 자체의 무게가 깎인다.**
- 같은 캡처의 타임스탬프가 **`2024-08-27 11:03`** 이다. 사이트 전체는 2026년 기준인데 이 그림만 2년 전이다.
- 조치: 상용 라이선스로 재생성하거나, 워터마크·날짜 영역을 크롭하거나, 네이티브 도해(`FIG`)로 다시 그린다.

## A-3. 접촉력 값이 자료마다 **다섯 가지**다 ★최우선

| 값 | 출처 | 맥락 |
|---|---|---|
| **1.5 N** | `sub.html` 본문 다수 · `crop_force.webp` | "목표 접촉력" |
| **3~8 N** | `sub.html` 본문 · `monitor.html`(3.0–8.0) | "유지 범위 / 목표대역" |
| **20 N** | `wide_system.webp` · `wide_loop.webp` | "수직 하중(N) 20.0" / "톨(N) 20.0" |
| **30 N** | `wide_result.webp` · `crop_monitor.webp` | "압력 30 N" / "하중: 30 N" |
| **68.3 N** | `crop_monitor.webp` | "Z-힘 추이 현재: 68.3 (N)", 축 최대 120 |

**가장 위험한 것은 68.3 N 이다.** `?p=safety` 는

> ISO/TS 15066 최저 한계(**안면 65 N**) 대비 상한 **12.3%**

라고 주장하는데, `?p=results` 에 실리는 `crop_monitor.webp` 는 **65 N 을 넘는 68.3 N** 을 실시간 대시보드로 보여준다. 안전 페이지와 결과 페이지가 정면으로 부딪친다.

## A-4. "목표 1.5 N" 이 "유지 범위 3~8 N" **바깥**에 있다

`sub.html:1604` · `1672` · `1764` 세 곳에서 반복되는 문장:

> 목표 접촉력 **1.5 N** 추종 · 유지 범위 **3–8 N** 검증 중

1.5 는 3~8 구간 안에 없다. 목표가 유지 범위 밖에 있다는 서술은 논리적으로 성립하지 않는다.
`crop_force.webp` 그래프도 **y축이 0~2.0** 이라 3~8 N 대역이 아예 그려지지 않는다.
반면 `monitor.html` 은 목표대역 3.0–8.0 에 로봇 목표값 5.6 / 5.2 / 4.5 를 쓴다.

→ **"목표"와 "유지 범위"의 정의를 먼저 정하고, 그다음 전 화면에 적용해야 한다.** 표기 통일보다 앞선다.

## A-5. 같은 "48개 학습 환경" 을 두 그림이 **완전히 다른 변수**로 설명한다

두 그림은 `?p=rl` 한 페이지에 **나란히** 실린다 (`sub.html:1771`, `1781`).

| | `wide_rl_overview.webp` (+ 본문 stats) | `crop_rl_env.webp` |
|---|---|---|
| 3종 | 경로 패턴 — 오비탈·스파이럴·리니어 | **표면 형상 — 평면·곡면·복합면** |
| 4단계 ① | 이송 속도 30·40·50·60 **%** | **속도 범위 500·800·1200·1500 RPM** |
| 4단계 ② | 누름 강도 1.0·1.5·2.0·2.5 N | **압력 범위 0.5·1.0·1.5·2.0 N** |

본문 `sub.html:1774-1777` 은 `wide_rl_overview` 쪽을 따른다. **`crop_rl_env.webp` 가 본문과 맞지 않는다.**

덤으로 **RPM 값도 4가지**다 — `?p=roadmap` "고정값 **3,000**", `crop_rl_env` "500~1500", `crop_monitor` "1200 rpm", `wide_result` "1,800 rpm".

## A-6. 웨이포인트 개수가 **세 가지**다

- 본문 전역: **813개** (`?p=results` stats, `?p=polishing` stats 등)
- `crop_force.webp` x축: **0 ~ 984**
- `wide_path.webp` ⑤ x축: **0 ~ 6K**

`TODO_사이트_누락콘텐츠` E섹션에 "사이트·기획서 813개 vs 보도자료 6,704개"로 이미 열려 있는 항목인데, **그림 두 장이 각각 또 다른 값을 그리고 있다.**

## A-7. `?p=media` 가 자기 문장을 스스로 부정한다

```
lede: '영상과 시연 자료. 전부 실제 실행 화면이다 — 연출 컷이 아니다.'
   ↓ 바로 아래 목록
VID('hero.mp4', '랜딩 히어로 필름 — 시네마틱 프로덕트 컷')
```

`sub.html:1855` 와 `sub.html:1860`. "연출 컷이 아니다"라고 선언한 목록에 "시네마틱 프로덕트 컷"이 들어 있다.
→ hero.mp4 를 빼거나, lede 를 "실행 화면과 브랜드 필름"으로 고친다.

## A-8. 연 구독료 산술이 맞지 않는다

`sub.html:1299` · `sub.html:1619` 모두 **"월 33만 원 · 연 393만 원"**.
33 × 12 = **396**. 393 이 아니다.

`TODO...md:207-209` 에 이미 열려 있는 항목이다 — 제출본은 393, 4.2·4.3 문서는 396.
어느 쪽을 쓰든 **월 단가와 연 단가가 서로 맞아야 한다.** (연 393만을 유지하려면 월 32.75만)

## A-9. 랜딩 「사양」 수치가 현재 배포본과 다르다

`index.html` 03 사양 섹션은 **"추정값은 싣지 않았다"** 는 헤드라인 아래 다음을 싣는다.

| 표기 | 실측 | 판정 |
|---|---|---|
| **2.15 MB** — 3D 모델 3종 합계 | robot_sl + car + sanding_kit = 2,200 KB | `ASSETS.md` 기준으론 **맞다** |
| **92.5 %** — 모델 용량 감축 | 위 3종의 28.69 MB → 2.15 MB | **맞다** |
| **171 KB** — 폰트 서브셋 3벌 | 57.39 + 57.04 + 56.86 = 171.3 KB | **맞다** |

문제는 **그 3종이 지금 배포되는 모델이 아니라는 것**이다.

- `robot_sl.opt.glb`(1.66 MB) 는 `.vercelignore` 로 **배포 제외** — "아무도 로드하지 않는다"고 명시돼 있다.
- `sanding_kit.opt.glb` 도 어느 화면에서도 로드하지 않는다.
- **실제 배포·로드되는 모델**: `car` + `robot_arm` + `lift` + `rail` = **4종 1,447 KB (1.41 MB)**

→ "추정값은 싣지 않았다"고 써 놓은 섹션이 **옛 자산 세트의 값**을 싣고 있다. 문구를 "4종 1.41 MB"로 고치거나, "감축 실적"임을 명시한다.

---

# B. 그림 안에 박힌 오타·비문

**모두 이미지 내부 텍스트다. 이미지를 다시 만들지 않으면 못 고친다.**

## B-1. `wide_loop.webp` — "**톨**(N)" 4회

"툴(Tool)" 의 오타로 보인다. ① 슬라이더 라벨, ① 하단 설명 "툴 (N)", ⑤ "경로 · **톨** · 속도 재설정", ⑤ 설명 "경로 / **톨** / 속도".
`crop_monitor.webp` 하단 상태바에도 **"톨: POL-125"** — **총 5회.**

> 참고: 단위가 N 이므로 "툴"보다 **"힘(N)"** 이 맞을 수도 있다. 무엇을 뜻하는지부터 정할 것.

## B-2. `wide_rl_overview.webp` — "**폴리토일** 환경 48종"

② 정책 학습 패널. `crop_rl_policy.webp` 의 같은 자리는 "폴리트원 환경"(A-1)이다. **같은 상자가 두 그림에서 각각 다르게 틀렸다.**

## B-3. `wide_system.webp` — 숙련공 말풍선이 비문이다

> "**항목 더 내기기에 대한 힘으로** 여러 번 지나가야 해요."

"내기기"는 단어가 아니다. 문장 전체가 성립하지 않는다.

## B-4. `wide_system.webp` — 지표 라벨 오타 3건

| 현재 | 추정 정답 |
|---|---|
| 표면**법** 편차 19% | 표면 **법선** 편차 |
| 경로 **충동도** 23% | 경로 **충족도** (또는 충돌도) |
| 보닛 중심부 편차 (**호차** 12.4%) | (**오차** 12.4%) |

## B-5. `wide_path.webp` ③ — "로봇의 **레일 후에도** 경로 유지"

"레일 **이동** 후에도" 가 빠졌다.

## B-6. `wide_system.webp` ① — 같은 라벨이 두 번, 값이 다르다

```
패드 직경 (mm)  ──── 150 mm
패드 직경 (mm)  ──── 800 mm
```

두 번째는 다른 항목(작업 폭·레일 길이 등)이어야 한다. **라벨 복붙 오류.**
참고로 `wide_loop.webp` 의 같은 자리는 "작업 영역 800 mm" 로 되어 있다 → **"작업 영역"이 맞다.**

## B-7. `wide_blindspot.webp` — "판정 원리 — **자동차단면**"

"자동차 단면" 으로 띄어야 한다. 붙여 쓰면 "자동 차단면"으로도 읽힌다.

## B-8. `crop_security.webp` — "→ **협력,경쟁사**에게 공유 가능"

쉼표 뒤 공백 누락 + "협력" 은 **"협력사"** 의 탈자. → `협력사 · 경쟁사에게 공유 가능`

## B-9. `wide_blindspot.webp` — 깨진 문자

수식 설명 두 번째 줄 앞에 **`—ㅓ →`** 라는 정체불명 문자가 있다. 글꼴 폴백 또는 편집 사고로 보인다.

## B-10. `crop_rbac.webp` — 같은 그림 안에서 "제어" vs "실행"

- 표 헤더: 조회 / 설정 / **제어**
- 바로 위 설명문: "조회/설정/**실행** 권한을 분리하여"

카피 쪽도 같이 흔들린다 → **D-6** 참조.

## B-11. `wide_blindspot.webp` — "**(b)** Blind Spot 진단"

원본 기획서의 (a)/(b) 중 (b)만 잘라온 흔적. 사이트에 (a)가 없으므로 의미 불명이다.

## B-12. `crop_preston.webp` — `MRR = kp · P · V`

Preston 계수는 **`k_p`**(아래첨자)다. 아래첨자가 소실돼 `kp` 로 붙어 있어 별개 변수처럼 읽힌다.
카피(`sub.html:1672`, `1758`)도 `MRR = kp·P·V` 로 같이 틀려 있다.

---

# C. 그림 자체의 문제

## C-1. 밝은 배경 이미지 2장이 다크 UI에서 튄다

| 파일 | 쓰이는 곳 | 상태 |
|---|---|---|
| `wide_docs.webp` | `?p=values` 대표 이미지 | ~~순백 배경~~ → **오판.** 투명 배경 webp 이며 브라우저에선 정상. (검수 뷰어의 투명→흰색 표시 때문) |
| `pdf_20_1746x1361.jpg` | `?p=values` 팀 사진 | 밝은 실사 + 흰 말풍선 |

나머지 22장은 전부 다크로 재작업돼 있다. **이 2장만 다른 사이트에서 온 것처럼 보인다.**

## C-2. `wide_blindspot.webp` 는 아래 30~40%가 빈 검정이다

선언 크기 1800×772 인데 실제 콘텐츠는 위쪽 일부뿐이다. `?p=polishing` 상세 영역에 큰 빈칸이 생긴다.
`crop_blindspot.webp` 도 좌우에 같은 여백이 있다. **크롭 필요.**

## C-3. `crop_rl_eval.webp` 의 인물이 AI 생성 티가 난다

- 작업복 두 사람의 **얼굴이 거의 동일**하다(같은 얼굴 복제).
- 안경 쓴 인물의 왼손이 노트북 뒤로 부자연스럽게 이어진다.

CLAUDE.md 가 통째로 경계하는 "AI가 만든 티"에 정확히 해당한다. 교체하거나 인물 없는 도해로 대체할 것.

## C-4. `pdf_20_1746x1361.jpg` 는 제품 톤과 맞지 않는다

- **카카오톡풍 흰 말풍선** 4개.
- 카피: "**꿈은 이루어진다!!**", "끝까지 열심히 하겠습니다!", "좋은 사람들과 좋은 경험" — 산업용 관제 제품의 톤이 아니다. **느낌표 2개**.
- 본문(`sub.html:1583`)의 "언젠가 실물 로봇 팔에도 적용해 볼 생각이다"와 말풍선 "김두용: 언젠가 실물 로봇 팔에도 적용해 볼래요."가 **같은 말의 반복**이다.
- 실명 4인 + 얼굴 노출 — 공개 게시 동의 확인 필요.
- 유일한 미변환 JPG(176 KB).

## C-5. 영문 라벨이 4곳에만 남아 있다

사이트는 전부 한글인데 다음만 영문이다.

| 파일 | 라벨 |
|---|---|
| `crop_preston.webp` | Polishing Pad · Polishing Object |
| `crop_rl_eval.webp` | Expert Evaluation |
| `crop_collision.webp` | COLLISION RISK |
| `wide_blindspot.webp` | Blind Spot (— 이건 본문에서도 영문이라 OK) |

## C-6. 캡션이 그림과 맞지 않는다 — 2건

| 위치 | 캡션 | 실제 그림 |
|---|---|---|
| `sub.html:1706` (`?p=sanding`) | "**래스터 경로 생성**" | `crop_path.webp` 는 **법선 축 + 등고선** 추정 그림이다. 래스터(지그재그) 경로가 아니다 |
| `sub.html:1688` (`?p=deburring`) | "**경계를 따르는 경로 생성**" | `crop_boundary.webp` 는 후드 **면** 위 오프셋 경로다. 디버링의 "모서리(엣지)" 경로가 아니다 |

## C-7. 같은 그림 + 같은 캡션이 두 페이지에 그대로 반복된다

`FIG('loop', '가상 작업장 파이프라인 — 10단계 중 04·05·06·07·10')`
→ `?p=goal`(L1563) 과 `?p=physical-ai`(L1756) 에 **문구까지 동일**하게 실린다.
(`?p=roadmap` L1744 는 캡션이 달라 문제없다.)

## C-8. 타사 상표가 식별 가능하게 노출된다

- `wide_system.webp` — 모델명 드롭다운에 **"BMW Z4"**. 게다가 그림 속 차량은 세단인데 Z4는 로드스터다.
- `wide_path.webp` ① / `crop_scan.webp` — BMW 그릴·엠블럼 식별 가능.
- `pdf_20...jpg` — **Samsung** 노트북 로고, 로봇 팔의 **두산로보틱스** 브랜드 링.

## C-9. `?p=news` · `?p=blog` · `?p=faq` 에 그림이 **0장**이다

18개 서브페이지 중 이 3개(+`newsletter`)만 시각 자료가 전혀 없다. FAQ는 8문답, Blog는 3편이라 분량은 있다.
의도라면 그대로 두되, PR 그룹 안에서 `media` 만 화려하고 나머지가 텍스트뿐이라 밀도가 튄다.

---

# D. 카피 표기 — 같은 것을 여러 방식으로 적는다

## D-1. 접촉력 범위 표기 3종 ★

| 표기 | 횟수 | 위치 |
|---|---|---|
| `3~8 N` | 10 | `sub.html` 본문 다수 |
| `3–8 N` (en dash) | 5 | `sub.html` stats · `?p=safety` |
| `3.0–8.0 N` | 1 | `monitor.html` "목표대역" |

**`?p=safety` 는 한 페이지 안에서 stats 는 `3–8`, 본문은 `3~8`** 을 쓴다.
같은 파일 안 다른 범위도 갈린다 — `50–70 µm`·`1.2–1.8 N` 은 en dash, `15~29세`·`3~5년` 은 물결표.

**정할 것:** 물리량 범위 = en dash / 나이·기간 = 물결표, 또는 전부 하나로.

## D-2. `150mm` vs `150 mm`

- 붙임 2회 — `sub.html:1601`, `sub.html:1666` ("150mm 패드 40% 중첩")
- 띄움 1회 — `sub.html:1871` (`?p=blog`, "150 mm 패드에 40% 오버랩이면 9 cm")

전체 통계로는 단위 띄움 35회 : 붙임 2회라 **띄움이 표준**이다.

## D-3. 숫자와 N 이 붙은 곳 2건

- [index.html:1248](index.html#L1248) — `접촉력 5.6N`
- [monitor.html:835](monitor.html#L835) — `목표 5.2N 추종 개시`

같은 `index.html` 안에서 라이브 세션 목업은 `접촉력 5.6 N` / `이송 30 mm/s`(띄움), 프리셋 목업은 `접촉력 5.6N` / `이송 30mm/s` / `오버랩 40%`(붙임)로 **바로 옆 컴포넌트끼리 다르다.**

## D-4. `ROS2` vs `ROS 2`

- `ROS 2` — `?p=physical-ai` 본문·stats, `wide_system.webp` 그림
- `ROS2` — `?p=results`(L1601 영역), `?p=news` 2026-08-10 항목

공식 표기는 **`ROS 2`** 다.

## D-5. `ANSYS` vs `Ansys`

`?p=physical-ai` 세 번째 스텝 **한 항목 안에서** 갈린다 — 설명문은 "**ANSYS** 물리 환경에서", 불릿은 "**Ansys** FEA 교차 검증", 캡션은 "**ANSYS** 사전 검증".
공식 표기는 **`Ansys`** 다.

## D-6. 권한 3단계가 "제어" 인지 "실행" 인지 흔들린다

| 위치 | 표기 |
|---|---|
| `?p=expert-db` 본문·불릿 | 조회 · 설정 · **제어** |
| `?p=faq` "핵심 데이터 보안은?" | 조회 · 설정 · **실행** |
| `crop_rbac.webp` 표 헤더 / 설명문 | **제어** / **실행** (같은 그림 안에서) |

## D-7. 존댓말과 평어체가 화면마다 다르다 ★

사이트 기조는 **평어체("~한다")** 다 — `sub.html` 0건, `monitor.html` 0건.
그런데 콘텐츠 서술문에서 존댓말이 나온다.

| 화면 | 문장 |
|---|---|
| ① 사전 설정 | "AI가 해당 하드웨어 조건에서 최적의 폴리싱 파라미터를 탐색**합니다**." |
| ① 사전 설정 | "…허용 오차 안에 들어야 라이브러리에 저장**합니다**." / "5개 항목 모두 허용 오차 안**입니다**." |
| ③ 결과 저장 | "…손잡이를 옮겨 기록**합니다**. 가운데가 동등**입니다**." |

`monitor.html` 은 같은 성격의 문장을 **"이 브라우저에서는 3D 뷰를 표시할 수 없다."** 로 쓴다.

**제안 규칙:** 폼 검증·에러 메시지만 존댓말(로그인 폼 7건은 지금도 그렇다), **그 외 모든 서술은 평어체.**

## D-8. 유효자리 불일치

- `?p=expert-db` — "접촉력 중앙값 **1.50** N"
- 그 외 전역 — "목표 **1.5** N"
- `crop_security.webp` 표 `contact_force_N` — `3.16` / `2.09` / `2.87` / **`1.2`** ← 마지막만 소수 1자리

CLAUDE.md 가 "실시간 수치에서 자릿수가 흔들리면 그것 하나로 아마추어가 된다"고 못박은 항목이다.

## D-9. 팀원 나열 순서가 다르다

- `?p=values` — 정용준 · 신현호 · 부승언 · 김두용
- `?p=news` 2026-08-10 — 김두용 · 신현호 · 부승언 · 정용준

---

# E. 기술·구조

## E-1. 폰트 서브셋에 **103자가 없어** Pretendard Full 이 통째로 내려온다 ★

`--f-sans: "Pretendard", "Pretendard Full", …` 구조라, 서브셋(825자)에 없는 글자가 하나라도 나오면
브라우저가 **`Pretendard-{300,500,700}.full-ko.woff2` (580 + 601 + 613 KB = 1.75 MB)** 를 받는다.

fontTools 로 실측한 결과 **모든 페이지가 해당된다.**

| 화면 | 누락 글자 수 | 예 |
|---|---|---|
| `sub.html` | 84 | 객 겠 겪 곤 긁 깅 깎 넷 논 닌 담 덮 돼 됐 될 뜻 띠 락 … |
| `console-src` | 16 | 겼 긋 깨 났 답 닿 둔 듀 듭 뜬 램 렴 령 … |
| `index.html` | 10 | 닫 **덜** 띄 맡 맨 및 받 았 책 **퇴** |
| `monitor.html` | 5 | 갠 끄 띄 잔 캐 |
| `library-src` | 5 | 답 릴 윕 줍 짚 |

**랜딩 첫 화면 카피에 직격한다.**
- "은**퇴**와 함께 사라진다" → `퇴` 없음
- "**덜** 지난 면은 **덜** 지난 대로 남는다" → `덜` 없음

기호도 빠져 있다 — `℃`(U+2103, `?p=roadmap` 온도 서술), `↔`(U+2194, `?p=safety`), `−`(U+2212, 마이너스).

**`ASSETS.md` 가 이미 경고한 상황이다:** "`.subset`은 …카피를 한 글자라도 바꾸면 그 글자가 두부(□)로 나온다."
현재 구조는 두부는 막았지만 **대신 1.75 MB 폴백이 매번 걸린다.** 폰트 예산 2 MB 를 거의 다 쓴다.

**선택지 2개**
1. 현재 카피 기준으로 **서브셋을 다시 뽑는다** (권장 — 171 KB 유지, 카피 고칠 때마다 재생성 필요)
2. `full-ko` 만 쓰고 subset 을 버린다 (1.75 MB 고정, 앞으로 뭘 써도 안전)

## E-2. `ASSETS.md` 안에서 서브셋 글리프 수가 어긋난다

- 표: "실제 쓰는 **825자**"
- 바로 아래 산문: "현재 텍스트에 등장한 **632자**만 들어 있다"

실측값은 **825자**(300/500/700 전부 동일)다. 산문 쪽을 고칠 것.

## E-3. 미참조 이미지 52개 · 35.1 MB

| 위치 | 개수 | 용량 | 배포 |
|---|---|---|---|
| `assets/new_img/` | 25 | **33.4 MB** | 제외됨 |
| `assets/img/` | 23 | 1.7 MB | **포함됨** ← `crop_knowhow.jpg`·`crop_loop.jpg`·`crop_params.jpg`·`crop_run.jpg`·`pdf_01`~`pdf_19` |
| `assets/icons/` | 4 | 12 KB | 포함됨 |

`assets/img/` 의 23개와 `icons/` 4개는 **배포에 실리는데 아무도 안 쓴다.** 지우거나 `.vercelignore` 에 넣을 것.

## E-4. `newsletter` 페이지가 고아다

`sub.html:1895` 에 페이지 데이터가 있는데 **메뉴·링크 어디에도 없다.** `?p=newsletter` 를 직접 쳐야 나온다.
`TODO...md` 에 "Newsletter 는 메뉴에서 뺐다"고 적혀 있으니, 데이터도 같이 지울지 결정할 것.

## E-5. `PH()` 빈 자리 6건 — 유지/제작 결정 필요

| 위치 | 페이지 | 캡션 |
|---|---|---|
| `sub.html:1691` | deburring | 디버링 실행 — 로드맵 4단계 · 힘 제어 모듈은 폴리싱과 공유 |
| `sub.html:1694` | deburring | 잔여 버 리포트 — 로드맵 4단계 · 리포트 형식은 폴리싱과 공유 |
| `sub.html:1709` | sanding | 입도 단계별 연마 — 로드맵 4단계 · 샌딩 킷 3D 자산만 준비됨 |
| `sub.html:1712` | sanding | 영역 단위 품질 판정 — 로드맵 4단계 · 판정 로직은 폴리싱과 공유 |
| `sub.html:1721` | pickplace | 위치·자세 추정 — 스캔 파이프라인 재사용, 이송 전용 캡처는 미제작 |
| `sub.html:1730` | pickplace | 사이클 타임 계측 — 이송 포함 전체 사이클, 캡처 미제작 |

캡션이 "미제작"이라고 솔직히 밝히고 있어 **그대로 둬도 정직하다.** 다만 `?p=sanding` · `?p=pickplace` 는 4칸 중 2칸이 빈 자리라 페이지가 비어 보인다.

---

# F. 이상 없었던 것

손댈 필요 없다.

- **이미지 참조 35건 전부 실물 존재.** `DIM` 선언 24개가 전부 실측 픽셀과 일치 — 깨진 그림·CLS 0건
- **금지 카피 0건** — `혁신적`·`직관적`·`seamless`·`powerful`·이모지 전부 없음
- **`transition: all` 0건**
- `tabular-nums` 54 · `word-break: keep-all` 38 · `:focus-visible` 11 · `prefers-reduced-motion` 14 — 5개 화면 전부에 적용
- **산술이 맞는 수치들** — `3 × 4 × 4 = 48` / 도달률 56.9% + 미도달 43.1% = 100 / 임계 곡률반경 75 mm = 패드 Ø150 의 반 / 150 mm × (1 − 0.4) = 9 cm / 대표 항목 22 = 완료 12 + 진행 5 + 예정 5 / 8 N ÷ 65 N = 12.3% / 클리어코트 50–70 µm 에 1회 5 µm·누적 40 µm 경고
- **`9시점`** 은 6곳 전부 일치 — `TODO...md:239` 의 "재확인" 항목은 **이미 해결됐다**
- `TODO...md` 미완료로 남아 있으나 **실제로는 반영된 항목 2건**
  - `:52` "하드웨어 CAGR 11.7% vs 검증 SW 37.6% = 3.2배" → `wide_market.webp` 에 이미 있다
  - `:115` "Processing 로봇 연 6천 대 = 전체의 2%" → `?p=faq` "왜 하필 연마인가?" 에 이미 있다

---

# 부록 — 작업 순서 제안

**1일차 · 이미지 재생성 (A·B)**
`wide_market` · `wide_loop` · `crop_rl_policy`(폴리트원) → `crop_ansys`(STUDENT) → `wide_system`(오타 4건) → `wide_rl_overview`(폴리토일) → `crop_monitor`(톨·68.3 N) → `wide_path` · `wide_blindspot` · `crop_security` · `crop_rbac`

**2일차 · 수치 확정 (A-3 ~ A-8)**
그림을 다시 만들기 전에 값부터 정해야 한다. 특히 **접촉력 정의(목표/유지 범위)** 와 **웨이포인트 수**, **48개 환경의 변수 3축**. `TODO...md` E섹션과 같이 처리할 것.

**3일차 · 카피 표기 통일 (D)**
`3~8` / `150 mm` / `ROS 2` / `Ansys` / `제어` / 평어체 — 규칙을 먼저 한 줄로 정하고 일괄 치환.

**4일차 · 자산 정리 (E)**
폰트 서브셋 재생성 → 미참조 이미지 정리 → `wide_blindspot` 크롭 → `wide_docs` · `pdf_20` 다크 대응 결정

---

## 재실행

```bash
python scripts/review-check.py --md    # 기계 점검 → review-report.md
python scripts/font-subset.py          # 카피를 고쳤으면 서브셋 재생성
```

폰트 커버리지·그림 대응표는 이 문서 작성용 일회성 스크립트로 냈다. 상시화가 필요하면 `scripts/` 로 옮길 것.
