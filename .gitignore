node_modules/
.vercel/
.env
.env.*

# 로컬 DB — 절대 커밋하지 않는다
data/*.db
data/*.db-shm
data/*.db-wal

# 작업 백업 — 저장소에 들어올 물건이 아니다
*.backup-*.html
*.html.bak
_snapshot_*/
_probe.html

# 참고용 원본 소스 (자체 git 저장소 — 이 repo 에 넣지 않는다)
polytwin_sourcecode/
