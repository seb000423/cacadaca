# PolyTwin 디자인 시스템

**작성** 2026-08-27 · **근거** `PolyTwin.html` 외 7개 번들에서 실제 사용 중인 값을 추출해 정리

> 이 문서는 상상이 아니라 **감사(audit) 결과**다. "현재"는 코드에서 실측한 값이고,
> "확정"은 앞으로 쓸 값이다. 둘이 다른 항목은 바꿔야 할 것이다.

---

## 0. 톤

산업용 계측 장비. 람보르기니 공식 사이트의 암전과 Linear 대시보드의 정보 밀도가
한 제품에 공존한다. **"미래적"이 아니라 "정밀한".** SF 아님.

- ⓪ 랜딩 = 인상. 여백 과잉, 한 화면에 문장 하나.
- ①–④ 콘솔 = 작업. 정보 밀도 최대, 여백 최소.
- 이 둘의 밀도가 비슷해지면 둘 다 실패한 것이다.

---

## 1. 색

### 현재 상태 (실측)

`PolyTwin.html` 전체에서 발견된 색상값 — 괄호 안은 출현 횟수.

| 역할 | 발견된 값 | 판정 |
|---|---|---|
| 표면 | `#08090B`(12) `#0B0D10`(7) `#0E1216`(5) `#101418`(4) `#14181D`(36) `#1A1E24`(8) | 6단계 — **너무 많다** |
| 경계 | `#232830`(27) `#2E343C`(6) `#3B424C`(9) | 3단계 — 적정 |
| 텍스트 | `#4A5058` `#5F6873`(81) `#6E7681` `#A8B0BA`(34) `#F2F4F7`(60) | 5단계 — **3개는 서로 구분 안 됨** |
| 액센트 | `#4FC3F7`(28) `#E8232A`(8) `#7ED957`(12) `#3DD68C`(3) `#FFB74D`(9) `#FFA726`(2) | **6개 — 심각** |

**문제 1.** 액센트가 6개다. 시안·빨강·초록 2종·앰버 2종. 화면에서 무엇이 중요한지
색으로 구분이 안 된다. `#7ED957`/`#3DD68C`는 같은 역할이고 `#FFB74D`/`#FFA726`도 같다.

**문제 2.** `#4A5058` `#5F6873` `#6E7681`은 다크 배경에서 육안 구분이 안 된다.
3단계인 척하는 1단계다.

**문제 3.** 표면 6단계는 그림자 없는 다크 UI에서 위계를 만들지 못한다. `#0E1216`과
`#101418`의 차이를 심사위원 노트북에서 볼 수 있는 사람은 없다.

### 확정 토큰 — 아래 15개만 쓴다

```css
:root {
  /* 표면 — 4단계로 축소 */
  --bg-void:    #08090B;  /* 랜딩 배경, 최심부 */
  --bg-base:    #0E1216;  /* 콘솔 바탕 */
  --bg-panel:   #14181D;  /* 패널·카드 */
  --bg-raised:  #1A1E24;  /* 호버, 선택된 행 */

  /* 경계 — 그림자 대신 선을 쓴다 */
  --line:       #232830;  /* 기본 1px */
  --line-hi:    #3B424C;  /* 강조·포커스 테두리 */

  /* 텍스트 — 3단계 */
  --text-hi:    #F2F4F7;  /* 제목, 수치 */
  --text-mid:   #A8B0BA;  /* 본문 */
  --text-lo:    #5F6873;  /* 라벨, 캡션, 단위 */

  /* 액센트 — 1개. 이것만 "브랜드"다 */
  --accent:     #3E9DBE;  /* 톤 낮춘 청록 (구 #4FC3F7에서 채도 −22%) */
  --accent-dim: #2A6478;  /* 비활성 상태 */

  /* 의미색 — 상태 표시 전용. 장식에 쓰면 의미가 죽는다 */
  --ok:         #3DD68C;
  --warn:       #FFB74D;
  --crit:       #E8232A;
}
```

> **`#4FC3F7`를 버리는 이유**
> Material Design의 Light Blue 300이다. SF 영화 UI와 AI 생성 대시보드의 기본값이라
> "미래적"이 아니라 "본 적 있는"으로 읽힌다. 채도만 낮춰도 계측 장비 쪽으로 넘어온다.

> **삭제 대상**
> `#7ED957` → `--ok`로 통합 · `#FFA726` → `--warn`으로 통합
> `#0B0D10` `#101418` → `--bg-base`로 통합 · `#4A5058` `#6E7681` → `--text-lo`로 통합
> `#2E343C` → `--line`으로 통합

### 규칙

- CSS에 리터럴 색상값(`#`으로 시작하는 것) 금지. 위 토큰만.
- `--crit`(빨강)은 **경고에만.** 랜딩 장식선에 쓰지 마라 — 지금 랜딩 하단에 쓰이고 있다.
- 의미색은 액센트가 아니다. 액센트는 `--accent` 하나뿐이다.

---

## 2. 타이포그래피

### 현재

**Pretendard** — 9 웨이트를 woff2 + woff 두 벌씩, 총 **24개 파일 16.26 MB**를 번들에 넣고 있다.
이게 랜딩 파일이 22.9 MB인 이유의 절반이다.

### 확정

```css
--f-sans: Pretendard, -apple-system, BlinkMacSystemFont, "Malgun Gothic", sans-serif;
--f-mono: "IBM Plex Mono", ui-monospace, Consolas, monospace;
```

**웨이트는 3개만 싣는다: 300 / 500 / 700.** 9개를 다 쓰고 있지 않다.
**`.woff`는 전부 버린다.** woff2를 못 읽는 브라우저는 2016년 이전 것뿐이다. (−9.6 MB)

| 역할 | 크기 | 웨이트 | 자간 | 행간 |
|---|---|---|---|---|
| 랜딩 워드마크 | 72–96px | 300 | `0.35em` | 1.0 |
| 랜딩 태그라인 | 16px | 300 | `0` | 1.7 |
| 콘솔 섹션 제목 | 13px | 500 | `0.14em` 대문자 | 1.4 |
| 콘솔 본문 | 13px | 400 | `0` | 1.5 |
| 수치 (대형) | 32–40px | 300 | `-0.03em` | 1.0 |
| 수치 (표) | 13px | 400 | `0` | 1.4 |
| 라벨·단위 | 11px | 500 | `0.12em` 대문자 | 1.3 |

**철칙**
- 숫자가 세로로 정렬되는 모든 곳에 `font-variant-numeric: tabular-nums`.
  실시간으로 바뀌는 수치에서 자릿수가 흔들리면 그 하나로 아마추어가 된다.
- 수치와 단위는 폰트를 나눈다. 값은 mono, 단위는 sans에 `--text-lo`.
- 한글 본문은 자간 `0`. 영문 대문자 라벨에만 자간을 준다.
- `word-break: keep-all` — 한글이 단어 중간에서 끊기지 않게.

---

## 3. 형태와 간격

```css
--r-sm: 2px;   /* 칩, 배지, 인풋 */
--r-md: 4px;   /* 패널, 카드 */
--r-lg: 0;     /* 풀블리드 섹션 — 라운드 없음 */
```

- **라운드는 4px를 넘지 않는다.** 12px 라운드는 소비자용 앱의 언어다.
- 간격은 4px 배수. `4 / 8 / 12 / 16 / 24 / 32 / 48 / 64 / 96`.
- **깊이는 그림자가 아니라 선으로.** 다크 UI에서 그림자는 보이지 않는다.
  `border: 1px solid var(--line)` + 배경 한 단계 밝게.
- 그림자를 꼭 써야 하면 검정 알파가 아니라 `#05070A` 계열로, 3단 이상.

### 밀도

| | 랜딩 ⓪ | 콘솔 ①–④ |
|---|---|---|
| 본문 | 16px / 1.7 | 13px / 1.5 |
| 섹션 여백 | 100vh | 16–24px |
| 한 화면 정보량 | 문장 1개 | 지표 6종 + 차트 3개 + 3D |

---

## 4. 모션

```css
--e-out: cubic-bezier(0.16, 1, 0.3, 1);   /* 진입 */
--e-in:  cubic-bezier(0.4, 0, 1, 1);      /* 이탈 */
--t-fast: 120ms;   /* 호버, 포커스 */
--t-base: 200ms;   /* 패널 전환 */
--t-slow: 400ms;   /* 화면 전환 */
```

- `transform`과 `opacity` 외의 속성은 애니메이트하지 않는다. `transition: all` 금지.
- 목록·카드 등장은 40ms씩 stagger.
- `prefers-reduced-motion: reduce`에서 전부 정지. 예외 없음.
- **차트 데이터 갱신에는 애니메이션을 넣지 마라.** 실시간 값이 매번 이징을 타면
  현재값을 읽을 수 없다. 값은 즉시, 형태만 부드럽게.

### 랜딩 스캔 시퀀스

원안 8초 무한 루프 → **1회 재생 후 유광 상태로 정지**로 변경한다.
루프는 두 번째 재생부터 배경 소음이 되고, 심사위원이 질문하는 동안 화면이 계속 깜빡인다.
재생 트리거: 최초 진입 1회 + 히어로 클릭 시 재생.

---

## 5. 3D (three.js r169)

### 현재 자산

| 모델 | 내부 이름 | 삼각형 | 원본 | 압축 후 |
|---|---|---|---|---|
| 로봇 SL | `m0609_SL_runtime` | 641,724 | 22.19 MB | **1.66 MB** |
| 차체 | `tc` (car_paint 재질 11종) | 163,636 | 4.00 MB | **0.26 MB** |
| 샌딩 킷 | `sanding_kit` | 111,387 | 2.50 MB | **0.23 MB** |

**Blender에서 그대로 뽑은 원본이었다.** 텍스처 0장, 압축·양자화·간략화 전부 없음.
`weld → simplify(50%) → quantize → meshopt`을 걸어 **28.69 MB → 2.15 MB (−92.5%)**.
실측 과정과 재현 방법은 `ASSETS.md`, 육안 검수는 `asset-check.html` 참고.

> 파일명이 뒤바뀌어 있었다. 22 MB짜리가 차체인 줄 알았으나 실제로는 SL 로봇이고,
> 차체는 4 MB짜리다. `assets/models/`에서 바로잡아 두었다.

### 렌더링 설정

```js
renderer.toneMapping = THREE.ACESFilmicToneMapping;  // 필수
renderer.toneMappingExposure = 1.1;
renderer.outputColorSpace = THREE.SRGBColorSpace;

// 환경맵 없이는 금속이 금속으로 안 보인다
const pmrem = new THREE.PMREMGenerator(renderer);
scene.environment = pmrem.fromScene(new RoomEnvironment(), 0.04).texture;
```

- 조명: 상단 대형 area light 1 + 좌우 rim light 2. **형상보다 조명이 중요하다.**
- 카메라: 지면에서 15–20° 올려다보는 각. 차가 크게 보인다.
- 무광 → 유광: `material.roughness` 0.8 → 0.15 를 스캔 라인 통과 위치에 맞춰 보간.
- 바닥: 아주 옅은 반사, 부드러운 접지 그림자.
- bloom은 아주 약하게(`strength` 0.25 이하). 강하면 즉시 게임 UI가 된다.

---

## 6. 성능 예산

| 항목 | 예산 | 현재 |
|---|---|---|
| 초기 HTML | < 300 KB | 8.1 MB ❌ |
| 3D 모델 합계 | < 3 MB | 28.7 MB ❌ |
| 폰트 | < 400 KB | 16.3 MB ❌ |
| 전체 최초 로드 | < 2 MB | 46.7 MB ❌ |
| LCP | < 2.5 s | 미측정 |

**심사 데모에서 로딩이 5초 걸리면 그 5초가 첫인상이다.**

---

## 7. 접근성

- 본문 대비 4.5:1 이상. `--text-lo`(`#5F6873`)를 `--bg-panel` 위에 쓰면 **3.1:1로 미달**이다.
  캡션·비활성 텍스트에만 쓰고 읽어야 하는 본문에는 `--text-mid` 이상.
- 모든 인터랙티브 요소에 `:focus-visible` 링 — `outline: 2px solid var(--accent); outline-offset: 2px`.
- 3D 캔버스에는 `aria-label`로 무슨 장면인지 설명.
- 상태를 색으로만 표시하지 않는다. `--crit` 빨강 옆에 아이콘이나 텍스트를 함께.

---

## 8. 금지 목록

이 프로젝트에서 하지 않는 것.

- 보라/인디고 그라디언트
- Inter, Space Grotesk
- 이모지 아이콘 (✨🚀⚡)
- 3열 균등 피처 카드
- `transition: all`
- 순수 회색 (`#808080`, `#F5F5F5`) — 중립색에 청색 5–10% 편향
- 장식용 `backdrop-blur`
- 12px 이상 라운드
- 형용사 나열 카피 — "seamlessly", "powerful", "intuitive", "혁신적인", "직관적인"
- 액센트 색 추가 — 새 색이 필요하면 기존 토큰으로 안 되는 이유를 먼저 적어라
