# 폴리트윈 UI 목업 프롬프트

**작성일** 2026-08-25 · **용도** Claude Code / v0 / Lovable 등에 붙여넣어 웹 UI 목업 생성
**전제** 기존 `web_dashboard/`(Vite 8 + Chart.js 4)가 이미 있음. 이건 그 **리디자인 + 기능 확장** 목업이다.

---

## 0. 쓰는 법

아래 `=== 프롬프트 시작 ===` 부터 `=== 프롬프트 끝 ===` 까지를 통째로 복사해서 붙여넣으면 된다.

**단계적으로 쓰는 걸 권장한다.** 한 번에 5화면을 다 시키면 품질이 떨어진다.

1. 1차: 프롬프트 전체를 주되 **"먼저 화면 ⓪만 만들어라"**라고 덧붙인다
   → ⓪ 랜딩이 디자인 언어를 결정하므로 여기부터 하는 게 좋다
2. 마음에 들면 **"같은 디자인 언어로 화면 ①"** — 이렇게 하나씩
3. 다 나오면 **"다섯 화면을 하나의 SPA로 합치고, ⓪은 풀스크린 랜딩, ①~④는 좌측 네비게이션"**

> **⓪ 랜딩부터 만드는 이유:** 색·타이포·모션의 톤이 여기서 정해진다. 콘솔 화면(①~④)은 그 톤을 정보 밀도가 높은 형태로 옮기는 것이라, 순서를 반대로 하면 랜딩이 대시보드처럼 밋밋해진다.

---

## 0-1. 지금 대시보드에 이미 있는 것 (중복 개발 방지)

| 기능 | 상태 |
|---|---|
| 파라미터 슬라이더 (`paramForce`, `paramSpeed`, `paramRPM`, `paramPad`, `paramPreston`) | **있음** |
| 로봇별 실시간 힘 차트 (C / SL / SR) | **있음** (Chart.js) |
| 커버리지 3D 캔버스, 히트맵, 카메라 뷰 | **있음** |
| 지표 6종 표시 (완료율, 접촉비율, 평균힘, 제거량, 총시간) | **있음** |
| 전/중/후 3단계 네비게이션 (`navPre`/`navDuring`/`navPost`) | **있음** |
| 시작·스캔·분석·연결 버튼 | **있음** |
| **숙련공 데이터 저장** | **없음 ← 신규** |
| **저장 데이터 불러오기·비교** | **없음 ← 신규** |
| **three.js 기반 진짜 3D 차량 뷰어** | **없음** (현재는 커스텀 캔버스) |

현재 디자인은 **라이트 테마 + 시안/오렌지 그라디언트**(`#f8fafc` 배경, `#4FC3F7`, `#FF9800`)다.
레퍼런스로 든 BMW·람보르기니는 정반대의 **다크 시네마틱**이므로, 이 목업은 **다크로 전환**하는 방향이다.

---

=== 프롬프트 시작 ===

## 만들 것

**PolyTwin(폴리트윈)** — 자동차 차체를 로봇팔이 자동 폴리싱하는 시뮬레이션의 **관제·설정 웹 콘솔** 목업.

산업용 디지털 트윈 제품이다. 사용자는 공정 엔지니어이고, 이 화면 앞에서 **파라미터를 정하고 → 공정을 지켜보고 → 잘 된 설정을 저장하고 → 나중에 다시 꺼내 쓴다.**

정적 목업이지만 **더미 데이터로 실제처럼 동작해야 한다** — 슬라이더를 움직이면 3D가 반응하고, 차트가 흐르고, 저장 버튼이 목록에 항목을 추가한다.

---

## 화면 5개

> ⓪은 제품 랜딩(브랜드) 화면이고, ①~④가 실제 작업 콘솔이다.
> ⓪에서 `콘솔 열기`를 누르면 ①로 진입한다.

---

### ⓪ 랜딩 — 시네마틱 인트로

**람보르기니 공식 사이트의 첫 화면 감성.** 이 화면은 기능이 아니라 **인상**을 만든다. 심사위원이 처음 보는 화면이고, 여기서 "이거 제품이네"라는 인상이 결정된다.

**전체 원칙**
- 배경은 거의 검정(`--bg-void`). 화면 끝까지 꽉 차는 풀블리드
- UI 크롬을 최소화 — 상단에 워드마크와 얇은 텍스트 링크 몇 개뿐. 박스형 버튼 금지
- **여백이 압도적으로 많아야 한다.** 요소를 채우려 하지 마라
- 스크롤에 따라 섹션이 하나씩 전환되는 **풀스크린 스크롤 구조** (각 섹션 100vh)
- 카피는 짧게. 한 섹션에 문장 하나

---

#### 섹션 1 — 히어로 (100vh) ★ 이 화면의 핵심

**차가 천천히 돌면서, 스캔 라인이 차체를 훑고 지나가면 표면이 무광에서 유광으로 바뀐다.**

이게 이 제품이 하는 일 그 자체다. 그냥 차가 도는 애니메이션이 아니라 **제품의 가치 제안을 그대로 보여주는 인트로**여야 한다.

**애니메이션 시퀀스 (총 8초, 무한 루프)**

```
0.0s   암전. 차체 실루엣만 희미하게
0.5s   하단에서 위로 조명이 켜지듯 차체가 드러남 (무광 상태, 어두운 회색)
1.5s   차체가 천천히 좌→우 턴테이블 회전 시작 (전체 구간 유지, 20초에 1바퀴)
2.0s   ★ 스캔 라인이 차체 앞→뒤로 훑고 지나감
         · 밝은 시안색 수평 라인, 두께 2~3px, 옅은 글로우
         · 라인이 지나간 자리는 무광 → 유광으로 변함 (roughness 0.8 → 0.15)
         · 유광이 된 면에 조명 반사 하이라이트가 생김
3.5s   스캔 라인 소멸. 차체 전체가 유광 상태
4.0s   타이포그래피 페이드인 (아래에서 12px 올라오며)
6.5s   페이드아웃 시작
8.0s   루프 재시작
```

**3D 구성**
- three.js. 차체는 단순 형상 조합(박스·실린더·곡면 평면)으로 실루엣만 잡아도 된다 — **조명과 재질이 형상보다 중요하다**
- 조명: 상단 대형 area light 1개 + 좌우 rim light 2개. 강한 명암 대비
- 바닥: 아주 옅은 반사가 있는 검은 평면. 차 밑에 부드러운 그림자
- 카메라: 살짝 낮은 각도(지면에서 15~20°)에서 올려다보는 구도 — 차가 크고 위압적으로 보인다
- 배경에 옅은 방사형 그라디언트로 차 뒤쪽만 아주 살짝 밝게 (비네팅 반대)

**타이포그래피 오버레이 (차 위에 겹침)**

```
        P O L Y T W I N
   ─────────────────────────
   숙련공의 손끝을 데이터로

        [ 콘솔 열기 ]
```

- 워드마크 `POLYTWIN`: 72~96px, weight 200~300, `letter-spacing: 0.35em`, 흰색
- 그 아래 가는 가로선 (폭 200px, 1px, `--text-lo`)
- 태그라인: 16px, `--text-mid`, `letter-spacing: 0.05em`
- `콘솔 열기`: **박스 버튼이 아니라 얇은 테두리 아웃라인**. 1px `--text-lo` 테두리, 넓은 패딩(18px 44px), 호버 시 테두리가 `--accent`로 바뀌고 배경이 아주 옅게 붉어짐
- 텍스트는 차체 중앙이 아니라 **약간 왼쪽 또는 아래쪽**에 배치해서 차와 겹치되 가리지 않게

**하단 스크롤 인디케이터**: 가는 세로선이 위아래로 천천히 움직이는 애니메이션 + `SCROLL` 라벨(10px, 자간 0.3em)

---

#### 섹션 2 — 3단계 공정 (100vh)

스크롤하면 **카메라가 차 주변을 이동하면서** 3단계를 하나씩 보여준다.

| 스텝 | 카메라 | 카피 |
|---|---|---|
| `01` SCAN | 차 위쪽으로 올라가 부감 | **스캔한다** / 깊이 카메라가 차체 형상을 3D 점군으로 읽는다 |
| `02` PATH | 후드 근접, 얕은 각도 | **경로를 만든다** / 곡률과 기울기를 읽어 패드가 지나갈 길을 그린다 |
| `03` POLISH | 측면으로 돌아 로봇팔 등장 | **닦는다** / 부위마다 힘을 바꿔가며 균일하게 |

- 각 스텝에서 3D 뷰에 해당 요소가 나타남 — 01은 점군, 02는 래스터 라인, 03은 로봇팔+패드
- 번호 `01` `02` `03`은 매우 크게(120px+), weight 200, `--text-lo` 색으로 배경처럼 깔린다
- 제목은 32px weight 600, 설명은 15px `--text-mid`
- 좌우 교대 배치 (01은 좌측 텍스트, 02는 우측, 03은 좌측)

---

#### 섹션 3 — 수치 (100vh)

**큰 숫자 네 개만.** 스크롤로 뷰에 들어오면 0부터 카운트업(800ms).

```
   3.23%              0회               96.2%             7대
   힘 변동계수         과압 발생          표면 커버리지       동시 제어 로봇
   숙련공 기준의 1/3    도장면 손상 0      빈 곳 없이         레일·측면·천장
```

- 숫자: JetBrains Mono, 64~80px, weight 300, `tabular-nums`, 흰색
- 라벨: 13px, `--text-mid`
- 부연: 11px, `--text-lo`
- 4열 그리드, 열 사이에 1px 세로 구분선(`--border-soft`)
- 배경은 완전한 검정. 3D 없음. **이 섹션만 정적이어서 리듬이 생긴다**

---

#### 섹션 4 — 마무리 CTA (60vh)

- 배경에 차체가 아주 어둡게, 흐릿하게(blur) 깔림
- 중앙에 한 문장: `지금, 콘솔에서 직접 조정해 보세요`
- 아래 `콘솔 열기` 아웃라인 버튼 (섹션 1과 동일 스타일, 조금 더 크게)
- 최하단에 얇은 푸터: `CACADACA · 협동3` · `2026 K-DT 해커톤` (11px, `--text-lo`)

---

#### 랜딩 화면 전용 주의사항

- **스캔 라인 애니메이션이 이 화면의 전부다.** 여기에 시간을 쓰고 나머지는 절제하라
- 자동재생 배경 영상 금지 (파일이 없다). 전부 three.js 실시간 렌더
- 파티클, 별, 떠다니는 도형 등 **장식 요소 금지**
- 스크롤 하이재킹은 하되 과하지 않게 — 휠 한 번에 한 섹션, 250ms 이내 전환
- 상단 네비게이션은 스크롤 시작하면 사라지고, 위로 스크롤하면 다시 나타남
- 모바일 대응 불필요 (데스크톱 1440px 기준)

---

### ① 차량 3D + 파라미터 설정 (메인)

**레이아웃:** 화면의 주인공은 **중앙의 대형 3D 차량 뷰포트**다. BMW 컨피규레이터처럼 차가 크게 자리를 차지하고, 컨트롤은 주변부로 밀려나 있어야 한다. 뷰포트가 화면의 최소 60%.

**3D 뷰포트 (three.js)**
- 차체 메시를 궤도 회전 / 줌 / 팬 (OrbitControls)
- 차체 표면에 **폴리싱 경로(래스터 지그재그)를 라인으로 오버레이**
- 로봇 3대(천장 C, 좌측 SL, 우측 SR)를 단순 형상(박스+실린더)으로 배치 — 정밀 모델 불필요
- 하단에 스튜디오 조명 느낌의 그림자 평면, 옅은 그리드
- 우하단에 뷰 프리셋 버튼: `정면` `상면` `측면` `자유`

**파라미터 패널 (우측, 폭 320~360px, 반투명 다크 글래스)**

파라미터를 바꾸면 **3D에 즉시 반영**되어야 한다 — 이게 이 화면의 핵심 인터랙션이다.

| 파라미터 | 컨트롤 | 범위 | 3D 반영 |
|---|---|---|---|
| 목표 접촉력 — 상면 평탄 | 슬라이더 | 3.0 – 12.0 N | 경로 라인 두께·색 강도 |
| 목표 접촉력 — 상면 급경사 | 슬라이더 | 3.0 – 12.0 N | 〃 |
| 목표 접촉력 — 측면 평탄 | 슬라이더 | 3.0 – 12.0 N | 〃 |
| 목표 접촉력 — 측면 급경사 | 슬라이더 | 3.0 – 12.0 N | 〃 |
| 패드 강성 k | 슬라이더 | 200 – 900 N/m | — |
| 감쇠비 ζ | 슬라이더 | 0.8 – 1.3 | — |
| 이송속도 | 슬라이더 | 0.02 – 0.05 m/s | 경로 애니메이션 속도 |
| 패드 지름 | 슬라이더 | 80 – 160 mm | **경로 간격이 실시간으로 벌어지고 좁아짐** |
| 오버랩 | 슬라이더 | 0 – 60 % | **〃 (간격 = 지름 × (1−오버랩))** |
| 패드 회전수 | 슬라이더 | 1000 – 4000 RPM | — |

- 목표 접촉력 4개는 **하나의 그룹 카드**로 묶고, 위에 작은 곡선 그래프로 "기울기 0°→45°에 따라 힘이 어떻게 변하는지"를 보여준다
- 각 슬라이더 우측에 **현재값을 모노스페이스로 크게** 표시
- 패널 하단에 `프리셋 불러오기` `현재 설정 저장` `시뮬 시작` 버튼

**좌측 얇은 레일 (폭 72px)**: 차종 썸네일 선택 — BMW Z4 / 벤츠 쿠페 / 페라리 SF90. 선택 시 3D 차체 교체.

---

### ② 공정 모니터링

**시뮬레이션이 도는 동안 보는 관제 화면.** 정보 밀도가 높아야 한다 — 여기서는 UVC 같은 산업 관제판의 문법을 따른다.

**상단 스테이터스 바 (전체 폭)**
- 좌: 상태 배지 — `POLISH` (초록 점 + 라벨). 상태 종류: `HOME` `SLIDE` `APPROACH` `POLISH` `RETRACT` `DONE`
- 중앙: 전체 진행률 바 + `68.4%`
- 우: 경과시간 `00:14:32`, 남은시간 추정, `일시정지` `정지` 버튼

**좌측 2/3 — 3D 실시간 뷰**
- ①과 같은 3D 뷰포트인데, **닦인 영역이 실시간으로 색이 변한다** (빨강 → 하늘색)
- 로봇 3대가 경로를 따라 움직임
- 우상단에 카메라 전환 드롭다운 (`전체` `천장뷰` `좌측` `우측` `패드 클로즈업`)

**우측 1/3 — 데이터 패널 (세로 스택)**
1. **로봇별 접촉력 실시간 차트 3개** — C / SL / SR. 각 차트 위에 현재값 크게(`7.82 N`), 목표 대역(2.5~14.0N)을 옅은 밴드로 표시. 대역을 벗어나면 밴드가 붉게 점멸
2. **지표 타일 그리드 (2×3)** — 힘 변동계수 / 목표대역 체류율 / 접촉 유지율 / 커버리지 / 제거 균일도 / 과압 발생. 각 타일에 큰 숫자 + 작은 라벨 + 미니 스파크라인
3. **이벤트 로그** — 타임스탬프 + 메시지. 예: `00:12:04  SR  접촉 끊김 감지` / `00:13:41  C  과압 임계 근접 12.8N`

**하단 — 제거량 히트맵**: 차체 탑뷰를 그리드로 펼친 히트맵. 색이 진할수록 많이 제거된 것.

---

### ③ 숙련공 데이터 저장

**이번 공정 결과를 "레시피"로 남기는 화면.** 단순 CRUD가 아니라, **숙련공의 판단을 함께 기록**하는 게 핵심이다.

**좌측 — 이번 런 요약 (읽기 전용)**
- 차종·부위 썸네일, 실행 일시, 총 소요시간
- 사용한 파라미터 전체 (①의 값들을 표로)
- 결과 지표 6종 (②의 타일과 동일)
- 제거량 히트맵 축소판

**우측 — 저장 폼**
- `레시피 이름` (텍스트, 기본값 자동생성: `BMW Z4 · 후드 · 08-25 14:32`)
- `태그` (칩 입력: 차종 / 부위 / 소재 / 마감등급)
- `메모` (여러 줄)
- **`숙련공 평가` — 이 화면의 핵심 블록**
  - 5개 지표를 **두 개씩 짝지어 "어느 쪽이 더 중요한가"를 묻는 쌍대비교 슬라이더** (총 10쌍 중 대표 3~4쌍만 목업에 표시)
  - 각 슬라이더는 좌↔우 양끝에 지표명, 가운데가 "동등"
  - 예: `힘이 일정한 것` ←──●──→ `골고루 닦는 것`
  - 하단에 산출된 가중치 5개를 **도넛 차트 또는 가로 바**로 즉시 미리보기
  - 작은 회색 글씨로 `일관성비율 CR 0.07 — 양호`
- `이 설정을 표준으로 지정` 토글
- `저장` 버튼 (큰 레드 CTA)

> 이 쌍대비교 블록이 제품의 차별점이다. **시각적으로 가장 공들여 만들어라.**

---

### ④ 데이터 불러오기 / 비교

**저장된 레시피 라이브러리.**

**상단 — 필터 바**: 검색창 + 차종 / 부위 / 소재 / 기간 드롭다운 + `표준만 보기` 토글 + 정렬(최신순 / 품질순)

**본문 — 레시피 카드 그리드 (3열)**

각 카드:
- 상단에 차체 썸네일 (제거량 히트맵을 배경으로 깔면 좋다)
- 이름 + 태그 칩
- 핵심 지표 3개를 인라인으로 (`CV 3.2%` `커버리지 96%` `과압 0`)
- 우상단에 `표준` 배지 (해당하는 경우)
- 하단 액션: `불러오기` `비교에 추가` `⋯`

**비교 모드**
- 카드 2~3개를 선택하면 하단에서 **비교 트레이가 올라온다**
- `비교하기` 클릭 → 나란히 놓인 비교 뷰
  - 파라미터 차이를 **다른 값만 하이라이트**해서 표로
  - 지표 6종을 **레이더 차트 겹쳐서**
  - 제거량 히트맵을 나란히
  - 하단에 `이 설정 불러오기` 버튼 각각

**`불러오기`를 누르면 화면 ①로 이동하면서 슬라이더가 그 값으로 애니메이션되며 이동한다.** (이 트랜지션을 꼭 넣어라)

---

## 디자인 방향

레퍼런스 4개를 이렇게 해석해서 섞는다.

| 레퍼런스 | 링크 | 무엇을 가져오는가 | 어느 화면에 |
|---|---|---|---|
| **Lamborghini** | https://www.lamborghini.com/ko-en | **다크 시네마틱 베이스.** 거의 검정에 가까운 배경, 극단적 명암 대비, 넓은 자간의 얇은 대문자 라벨, 과감한 여백. 화면이 "제품 카탈로그"처럼 보여야 한다 | **⓪ 랜딩(주)** · 전체 톤 |
| **BMW 컨피규레이터** | https://www.bmw.co.kr/ko/configurator.html | **3D가 주인공인 레이아웃.** 차가 중앙에 크게, 컨트롤은 주변부 얇은 패널로. 옵션을 바꾸면 즉시 3D에 반영되는 즉각성 | **① 파라미터 설정(주)** |
| **UVC (유비씨)** | https://uvc.co.kr/ | **산업 관제의 정보 밀도와 신뢰감.** 상태 배지, 실시간 수치, 이벤트 로그. 브랜드 레드(#D20A11)를 단일 액센트로 쓰는 절제. *제조 디지털 트윈 플랫폼 회사 — 우리와 같은 카테고리다* | **② 모니터링(주)** · ④ 라이브러리 |
| **three.js** | https://threejs.org/ | **3D 뷰포트의 기술적 미학.** 옅은 그리드, 미니멀한 컨트롤 오버레이, 모노스페이스 수치 표기 | 모든 3D 뷰포트 |

**레퍼런스를 볼 때 이렇게 보라**

- **Lamborghini** — 첫 화면의 여백 비율, 워드마크 자간, 스크롤 시 섹션이 넘어가는 속도. 색은 거의 검정 하나뿐이라는 것
- **BMW 컨피규레이터** — 옵션 패널의 폭과 위치, 차가 화면에서 차지하는 비율, 색상 칩을 눌렀을 때 3D가 바뀌는 딜레이(거의 0)
- **UVC** — 산업 제품 사이트가 신뢰감을 만드는 방식. 레드를 어디에만 쓰는지 세어볼 것
- **three.js** — 예제 페이지의 뷰포트 오버레이 UI. 정보를 최소한으로 얹는 방법

**핵심 원칙: 화려한 게 아니라 "비싸 보이는" 것이 목표다.** 그라디언트·글로우·네온을 남발하지 마라. 검정 바탕, 정확한 여백, 딱 하나의 붉은 액센트, 그리고 3D 렌더링 자체의 품질로 승부한다.

### 색

```
--bg-void      #08090B    화면 최하단 배경
--bg-surface   #101215    카드·패널
--bg-elevated  #171A1F    떠 있는 요소, 호버
--border       #232830
--border-soft  #1A1E24

--text-hi      #F2F4F7    제목·수치
--text-mid     #A8B0BA    본문
--text-lo      #5F6873    라벨·캡션

--accent       #E8232A    브랜드 레드 (CTA, 활성 상태, 강조) — 아껴 쓸 것
--accent-dim   #8C1418    레드의 어두운 변형 (배경 틴트)

--data-c       #4FC3F7    로봇 C (천장) — 시안
--data-sl      #7ED957    로봇 SL (좌측) — 그린
--data-sr      #FFB74D    로봇 SR (우측) — 앰버

--ok           #3DD68C
--warn         #FFA726
--danger       #FF4D4F    (브랜드 레드와 구분되도록 더 밝게)
```

> **레드는 액센트다. 화면당 3~4곳 이상 쓰지 마라.** CTA 버튼, 활성 탭, 임계 초과 알람 정도.

### 타이포그래피

- **본문·제목**: `Pretendard` (한글 필수. 프로젝트 에셋에 이미 있음. 없으면 Google Fonts의 `IBM Plex Sans KR`)
- **수치·코드**: `JetBrains Mono` (기존 대시보드와 동일)
- **섹션 라벨**: Pretendard 500, `font-size: 11px`, `letter-spacing: 0.14em`, `text-transform: uppercase`, 색은 `--text-lo`
  → 이 라벨 스타일이 람보르기니 감성의 핵심이다. 모든 패널 제목에 일관되게 적용
- **큰 수치**: JetBrains Mono 500, 28~40px, `font-variant-numeric: tabular-nums`
- 제목에 그림자·글로우 금지

### 레이아웃

- 좌측 고정 네비게이션 (폭 72px, 아이콘만) — 화면 4개 + 설정
- 컨텐츠 영역은 최대 폭 제한 없이 화면을 꽉 채운다 (관제 화면이므로)
- 패널 모서리 반경 `10px`, 테두리 `1px solid var(--border)`
- 패널 배경은 반투명 + `backdrop-filter: blur(12px)` — 단, **3D 뷰포트 위에 뜨는 패널에만.** 남발 금지
- 그림자는 아주 낮게 `0 2px 12px rgba(0,0,0,0.4)`

### 모션

- 전환은 `220ms cubic-bezier(0.4, 0, 0.2, 1)`
- 슬라이더 조작 시 3D 반영은 **지연 없이 즉시**
- 숫자가 바뀔 때 카운트업 애니메이션 (200ms)
- 페이지 전환은 크로스페이드 + 살짝 위로 (8px)
- **과한 등장 애니메이션 금지.** 관제 화면은 빨리 떠야 한다

---

## 채워 넣을 실제 데이터

**목업이라도 lorem ipsum이나 가짜 숫자를 쓰지 마라.** 아래 실제 값을 쓴다.

```
로봇 구성    천장 C, 좌측 SL, 우측 SR (+ 레일 4대는 목업에서 생략 가능)
차종         BMW Z4 / 벤츠 쿠페 / 페라리 SF90
부위         후드(본네트), 펜더, 루프, 도어

목표 접촉력   상면 평탄 8.0 N   상면 급경사 5.0 N
             측면 평탄 6.0 N   측면 급경사 3.5 N
             (기울기 0°→45° 사이를 선형 보간)
패드 강성     350 N/m
감쇠 계수     35
진행 허용대역  2.5 ~ 14.0 N
안전 한계     상면 14.0 N / 측면 6.0 N
패드          지름 110 mm, 회전 3000 RPM
이송속도       0.030 m/s
경로 간격      지름 × (1 − 오버랩).  110mm · 40% → 66mm

지표 현재값    힘 변동계수      3.23 %
             목표대역 체류율   100.0 %
             접촉 유지율      73.6 %
             커버리지         96.2 %
             제거 균일도       12.4 %
             과압 발생        0 회
             전이구간 CV      71.85 %   ← 개선 대상이라 붉게 표시

상태          HOME / SLIDE / APPROACH / POLISH / RETRACT / DONE

이벤트 로그 예시
  00:02:11  C   폴리싱 시작 · 구간 1/5
  00:07:38  SL  접촉 진입 · 목표력 6.0N
  00:12:04  SR  접촉 끊김 감지 — 재접근
  00:13:41  C   과압 임계 근접 12.8N — 감속
  00:18:22  —   구간 3/5 완료 · 커버리지 61.4%

저장된 레시피 예시 (4~6개)
  BMW Z4 · 후드 · 표준        CV 3.2% / 커버리지 96% / 과압 0
  BMW Z4 · 후드 · 고속안       CV 5.8% / 커버리지 91% / 과압 0
  벤츠 쿠페 · 루프            CV 4.1% / 커버리지 94% / 과압 1
  페라리 SF90 · 펜더 · 실험    CV 8.9% / 커버리지 88% / 과압 3
```

---

## 기술 레퍼런스

### three.js

| 용도 | 링크 |
|---|---|
| 공식 사이트 | https://threejs.org/ |
| 문서 (API 레퍼런스) | https://threejs.org/docs/ |
| 예제 모음 | https://threejs.org/examples/ |
| 입문 매뉴얼 | https://threejs.org/manual/ |

**예제 페이지에서 찾아볼 것** (좌측 사이드바 검색창에 키워드를 넣으면 나온다)

| 키워드 | 왜 보는가 |
|---|---|
| `car` | 차량 페인트 재질 예제. **클리어코트가 적용된 차체 재질 설정을 그대로 가져올 수 있다** |
| `clearcoat` / `physical` | `MeshPhysicalMaterial`의 clearcoat·roughness 조합. ⓪의 무광→유광 전환이 여기 달렸다 |
| `rectarealight` | 스튜디오 조명. 상단 대형 area light가 자동차 렌더의 핵심이다 |
| `orbitcontrols` | 궤도 카메라 컨트롤 |
| `envmap` / `pmrem` | 환경맵 반사. 유광 표면에 반사가 없으면 유광으로 안 보인다 |

**핵심 API** (문서에서 찾아볼 것)

```
THREE.MeshPhysicalMaterial     ← 차체 재질. roughness / metalness / clearcoat / clearcoatRoughness
THREE.RectAreaLight            ← 스튜디오 조명 (RectAreaLightUniformsLib 초기화 필요)
THREE.PMREMGenerator           ← 환경맵 생성. 반사를 만들려면 필수
OrbitControls                  ← addons/controls/OrbitControls.js
RoomEnvironment                ← addons/environments/RoomEnvironment.js — 빠른 기본 환경맵
```

> **⓪의 스캔 애니메이션 구현 힌트:** 차체 재질의 `roughness`를 통째로 바꾸지 말고,
> **셰이더에 스캔 라인의 y좌표를 uniform으로 넘겨서 그 아래/위를 다르게 렌더**하거나,
> 차체를 세로로 분할한 여러 메시로 만들어 순차적으로 재질을 바꾸는 방법이 간단하다.
> 후자가 구현이 쉽고 목업에는 충분하다.

### CDN

```html
<script type="importmap">
{
  "imports": {
    "three": "https://cdn.jsdelivr.net/npm/three@0.169.0/build/three.module.js",
    "three/addons/": "https://cdn.jsdelivr.net/npm/three@0.169.0/examples/jsm/"
  }
}
</script>
```

- Chart.js: `https://cdn.jsdelivr.net/npm/chart.js@4.4.6/dist/chart.umd.min.js`
- Pretendard: `https://cdn.jsdelivr.net/gh/orioncactus/pretendard/dist/web/static/pretendard.css`
- JetBrains Mono: Google Fonts

---

## 기술 제약

- **단일 HTML 파일**로 만든다. CSS·JS 인라인. 외부 이미지 금지(도형·CSS·SVG로 대체)
- **3D는 three.js** — 위 importmap 사용 + `OrbitControls`
- 차량 3D 모델이 없으므로 **박스·실린더·평면을 조합한 단순화된 차체 실루엣**으로 대체한다. 후드 곡면은 `PlaneGeometry`를 곡률 주어 변형
- 차트는 **Chart.js CDN** 또는 순수 canvas 직접 구현
- 폰트는 Google Fonts 링크 허용
- **더미 데이터가 실시간처럼 흘러야 한다** — `setInterval`로 힘 값이 목표 주변에서 노이즈를 갖고 진동, 진행률이 올라가고, 커버리지 색이 점점 칠해짐
- 다크 테마 고정 (라이트 모드 불필요)
- 데스크톱 1440px 기준. 반응형은 1280px까지만 고려

---

## 하지 말 것

- 라이트 테마
- 그라디언트 남발 (특히 보라-파랑, 청록-분홍 같은 AI 기본값 조합)
- 네온 글로우, `box-shadow`에 컬러 넣기
- 이모지 아이콘 — 아이콘은 인라인 SVG로 그린다
- 카드마다 다른 모서리 반경·여백
- 라벨 없는 숫자, 단위 없는 수치
- 화면 가장자리 여백 24px 미만
- 3D 뷰포트를 작게 만들기 — **이게 주인공이다**
- lorem ipsum, 가짜 회사명, 무의미한 더미 텍스트

=== 프롬프트 끝 ===

---

## 부록 — 변형해서 쓰는 법

### 더 짧게 (빠른 시안이 필요할 때)

"만들 것" + "화면 ①" + "디자인 방향의 색·타이포" + "하지 말 것"만 남기고 나머지를 잘라낸다.

### 이미지 레퍼런스를 함께 줄 때

BMW 컨피규레이터와 람보르기니 사이트를 **스크린샷 찍어서 프롬프트와 함께 첨부**하면 결과가 훨씬 좋아진다. 텍스트 설명보다 이미지 한 장이 정확하다.

### 기존 대시보드에 이식할 때

목업이 확정되면 이렇게 이어서 지시한다.

> "이 목업의 디자인 토큰(색·타이포·간격)을 `web_dashboard/src/style.css`의 CSS 변수로 옮기고, 기존 `index.html`의 마크업 구조는 유지한 채 스타일만 교체해라. Chart.js 설정도 새 팔레트에 맞춰 갱신해라."

### 랜딩(⓪)만 따로 뽑아 쓸 때

발표용 인트로 화면으로만 쓸 거라면 프롬프트에서 **"만들 것" + "화면 ⓪" + "디자인 방향" + "채워 넣을 실제 데이터의 지표 부분" + "하지 말 것"** 만 남기면 된다. ①~④는 통째로 잘라내도 ⓪은 독립적으로 성립한다.

이 경우 마지막에 이렇게 덧붙이면 좋다.

> "이 랜딩 페이지는 발표 시연의 첫 화면으로 쓴다. 로딩 후 3초 안에 스캔 애니메이션이 시작되어야 하고, 발표자가 스크롤하지 않아도 히어로 섹션만으로 제품이 뭔지 전달되어야 한다."

### 화면 ③④를 먼저 만들고 싶다면

①②는 이미 기존 대시보드에 있으므로, **신규 기능인 ③(저장) ④(불러오기)부터 만드는 것도 합리적이다.** 그 경우 프롬프트에서 ①②를 "기존 화면 참조"로만 남기고 ③④에 집중시킨다.

### 주의

**숙련공 쌍대비교(③) 블록은 L3 설계와 직결된다.** AHP로 가중치를 뽑는 그 절차를 UI로 만든 것이다. 목업에서 이 부분을 잘 만들어두면 나중에 실제 인터뷰 도구로도 쓸 수 있다.
